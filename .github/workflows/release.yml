name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          # This is Required, as @changesets/bot expects a full git history
          fetch-depth: 0

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Create Release Pull Request or Version
        id: changesets
        uses: changesets/action@v1
        with:
          title: "chore: release"
          commit: "chore: release"
          commitMode: github-api
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # - name: Publish to VS Code Marketplace
      #   if: steps.changesets.outputs.publishedPackages == '["vscode-changesets"]'
      #   run: |
      #     # Install vsce if not already installed
      #     npm install -g @vscode/vsce

      #     # Publish to marketplace
      #     vsce publish --pat ${{ secrets.VSCE_PAT }}